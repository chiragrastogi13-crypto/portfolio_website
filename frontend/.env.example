# Copy to .env to override. Defaults work for local dev.
VITE_API_BASE=http://127.0.0.1.nip.io:8000

# Production (set as a build env var in Netlify, NOT committed):
# VITE_API_BASE=https://api.wlelo.com
